meta {
  name: Create Service
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/services
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "serviceType": "REVISAO",
    "idOS": "{{serviceOrderId}}"
  }
}

