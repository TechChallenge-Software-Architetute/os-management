meta {
  name: Average Execution Time By Id
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/monitoring/by-id
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "id": "{{serviceId}}",
    "timeUnit": "MINUTES"
  }
}

